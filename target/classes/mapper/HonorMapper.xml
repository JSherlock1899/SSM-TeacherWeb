<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.slxy.edu.mapper.HonorMapper">

    <select id="selectAll" resultType="Honor" >
        select Hsn,Hname,Hwinner,Hdate,Hcompany,Hgrad,Hreward,Hremarks,Cname,Dname FROM Honor h JOIN Teacher t ON h.Tsn = t.Tsn JOIN College c on c.Csn = t.Csn JOIN  Sdept s ON s.Dsn = t.Dsn
        <where>
            <if test="tname!=null">
                Tname = #{tname}
            </if>
            <if test="cname!=null">
                AND Cname = #{cname}
            </if>
            <if test="dname!=null">
                AND Dname = #{dname}
            </if>
            <if test="starttime!=null">
                AND Hdate &gt;= #{starttime}
            </if>
            <if test="endtime!=null">
                AND Hdate &lt;= #{endtime}
            </if>
        </where>
    </select>



</mapper>